# GNN Model

This layout is designed to mirror SME, but limited to data relevant to this project. 

- Model checkpoints are stored in ./Model
- Hyperparameters in ./result
- Input structures in ./data
- Aggregated predictions in ./prediction/summary